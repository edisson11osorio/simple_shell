#include "shell.h"

int main()
{
    for (i = 0; *directories[i] != '\0'; i++)
    {
        if (function == 0)
        {
            return (0);
        }
        function = stat(directories[i], token);
    }
}
